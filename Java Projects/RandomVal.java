//Yixing Zheng
import java.util.*;
public class RandomVal {

	public static void main(String[] args) {
	int l = 100 + (int) (Math.random()* 10);
	int s = 100 + (int) (Math.random()* 10);
	int y = 100 + (int) (Math.random()* 10);

	System.out.println("The three random numbers are " + (l /10.0) + ", " + (s /10.0) + " ,and "  + (y /10.0) );
	}

}
